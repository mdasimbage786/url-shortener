# === PostgreSQL Database Configuration ===
spring.datasource.url=jdbc:postgresql://dpg-d2v69p15pdvs73b399ig-a.oregon-postgres.render.com:5432/databse_c8kg
spring.datasource.username=asimbage
spring.datasource.password=Yi4Pueas4QwE7P5OVCu9dfMmtenmnApK
spring.datasource.driver-class-name=org.postgresql.Driver

# === Hibernate / JPA Configuration ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Server Configuration
server.port=8080
server.servlet.context-path=/

# Logging Configuration
logging.level.com.mdasim.urlshortener=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Application Configuration
spring.application.name=URL Shortener
spring.profiles.active=dev

# Actuator Configuration (for monitoring)
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized
