# === PostgreSQL Database Configuration ===
spring.datasource.url=jdbc:postgresql://dpg-d3m008ogjchc73cp5tng-a.oregon-postgres.render.com:5432/db_nsto
spring.datasource.username=aeimbage
spring.datasource.password=6JwB0mDq0FovJfnd4eW1VwidTMIHsOXc
spring.datasource.driver-class-name=org.postgresql.Driver

# === Hibernate / JPA Configuration ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Server Configuration
server.port=8080
server.servlet.context-path=/

# Logging Configuration
logging.level.com.mdasim.urlshortener=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Application Configuration
spring.application.name=URL Shortener
spring.profiles.active=dev

# Actuator Configuration (for monitoring)
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized
