# === PostgreSQL Database Configuration ===
spring.datasource.url=jdbc:postgresql://dpg-d4mpi6vpm1nc73d4eoi0-a.oregon-postgres.render.com:5432/db4_62se
spring.datasource.username=asimbage
spring.datasource.password=Agx44eVY2vXu0JambKOuUxEkPxaWoQWW
spring.datasource.driver-class-name=org.postgresql.Driver

# === Hibernate / JPA Configuration ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Server Configuration
server.port=8080
server.servlet.context-path=/

# Logging Configuration
logging.level.com.mdasim.urlshortener=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Application Configuration
spring.application.name=URL Shortener
spring.profiles.active=dev

# Actuator Configuration (for monitoring)
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized
